import React from "react";
export default function Header(){
  return (
    <header className="header">
      {/* <h1>Alpha Student Project — UI + Contact API</h1>
      <p className="muted">Three sections implemented + a contact endpoint that saves messages to MongoDB.</p> */}
    </header>
  );
}
